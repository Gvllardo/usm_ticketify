import 'package:flutter/material.dart';

class ClienteEventosTerminados extends StatelessWidget {
  const ClienteEventosTerminados({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Eventos finalizados'),
    );
  }
}